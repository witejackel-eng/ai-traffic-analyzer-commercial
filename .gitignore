# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.vscode/
.idea/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (NEVER commit secrets)
.env
.env.local
.env.*.local

# database
/db/*.db
/db/*.db-journal
/prisma/migrations/dev.db*

# storage (uploaded videos + outputs)
/storage/
!/storage/.gitkeep

# data samples — keep the real traffic video fixture (CC0) + exclude large generated videos
/data/samples/test-traffic*.mp4
!/data/samples/real-traffic.mp4

# real-pipeline dataset — keep the validation videos (CC0) + visual proof artifacts
# but exclude raw inference JSON outputs
/tests/real-pipeline/dataset/results.json
/tests/real-pipeline/accuracy/ground_truth.json
/tests/real-pipeline/accuracy/accuracy_report.json
/tests/real-pipeline/accuracy/frames/

# logs
*.log
dev.log
server.log

# tool / scratch
/tool-results/
/download/
/skills/
/.zscripts/

# caddy (local dev gateway)
Caddyfile

# build scratch / agent coordination (not part of product)
/worklog.md
/upload/
/examples/

# test artifacts (keep the visual proof artifacts; ignore run output)
/test-results/
/tests/.playwright-report/
/tests/results/
/coverage/
/dist/
tests/.playwright-artifacts/
tsconfig.tsbuildinfo

# pytorch model (not used in production — replaced by the lighter ONNX SSD model)
mini-services/vision-worker/models/fasterrcnn_mobilenet_v3_coco.pth
